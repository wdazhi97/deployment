# helm-debug 环境：仅环境相关覆盖，其余用各子 Chart 默认
# 使用: helm install snake-game-debug ../snake-game -n helm-debug -f values.yaml --create-namespace

global:
  environment: debug

gateway:
  environment: debug

# 按需覆盖子 Chart（示例）
# mongodb:
#   replicas: 1
# lobby:
#   replicas: 1
