image: snake-game-lobby:latest
imagePullPolicy: Never
replicas: 1
port: 50051
command: ["/root/bin/lobby_server"]
# 由父 Chart 或环境 values 覆盖
mongodbUri: "mongodb://admin:password@mongodb:27017"

resources:
  requests:
    cpu: 10m
    memory: 64Mi
  limits:
    cpu: 100m
    memory: 128Mi
