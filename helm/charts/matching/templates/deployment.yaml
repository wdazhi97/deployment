apiVersion: apps/v1
kind: Deployment
metadata:
  name: matching
  labels:
    app: matching
spec:
  replicas: {{ .Values.replicas | default 1 }}
  selector:
    matchLabels:
      app: matching
  template:
    metadata:
      labels:
        app: matching
    spec:
      containers:
      - name: matching
        image: {{ .Values.image }}
        imagePullPolicy: {{ .Values.imagePullPolicy | default "IfNotPresent" }}
        ports:
        - containerPort: {{ .Values.port }}
        env:
        - name: MONGODB_URI
          value: {{ .Values.mongodbUri | quote }}
        command: {{ .Values.command }}
        livenessProbe:
          tcpSocket:
            port: {{ .Values.port }}
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          tcpSocket:
            port: {{ .Values.port }}
          initialDelaySeconds: 5
          periodSeconds: 5
        resources: {{- toJson .Values.resources | nindent 8 }}
---
apiVersion: v1
kind: Service
metadata:
  name: matching
  labels:
    app: matching
spec:
  selector:
    app: matching
  ports:
  - protocol: TCP
    port: {{ .Values.port }}
    targetPort: {{ .Values.port }}
  type: ClusterIP
